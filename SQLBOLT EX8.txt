Ques.1: Find the name and role of all employees who have not been 	assigned to a building.
Query : SELECT name, role
	FROM employees
	WHERE building IS NULL;

Ques.2: Find the names of the buildings that hold no employees.
Query : SELECT building_name
	FROM buildings
	LEFT JOIN employees
	ON buildings.building_name = employees.building
	WHERE employees.name IS NULL;